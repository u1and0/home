modules = ('numpy',
           # 'seaborn',
           'pandas',
           'matplotlib.pyplot')
standfor = {'numpy': 'np',
            'seaborn': 'sns',
            'pandas': 'pd',
            'matplotlib.pyplot': 'plt'}

for module in modules:
    execution = 'import %s as %s' % (module, standfor[module])
    print(execution)
    exec(execution)
